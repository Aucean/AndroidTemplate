<template format="4"
    revision="1"
    name="ViewModel Based Fragment Template"
    description="Create a ViewModel Based fragment template">
    
    <category value="VMFragment" />
    <formfactor value="Mobile" />
    
    <parameter id="className"
    name="Fragment Name"
    type="string"
    constraints="class|unique|nonempty"
    default="Name"
    help="The name of the functionality that requires ViewModel based fragment"/>
    
    
    <parameter
    id="fragmentLayoutName"
    name="Fragment Layout Name"
    type="string"
    constraints="layout|unique|nonempty"
    suggest="fragment_${classToResource(className)}"
    default="fragment_custom"
    help="The name of the layout to create for the fragment" />
    
    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />
</template>

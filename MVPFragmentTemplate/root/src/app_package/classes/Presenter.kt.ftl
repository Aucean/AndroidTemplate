package ${packageName}.presenter

import ${packageName}.activity.BaseAppCompatActivity
import ${packageName}.uiinterface.I${className}Ui

class ${className}Presenter(internal val mActivity: BaseAppCompatActivity,
                           internal val mView: I${className}Ui) : BasePresenter() {

    override fun start() {

    }

    override fun stop() {

    }
}
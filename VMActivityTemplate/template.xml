<template format="4"
    revision="1"
    name="ViewModel based activity template"
    description="Creates a new MVP classes - Presenter, View and Contract between the two">
    
    <category value="VMActivity" />
    <formfactor value="Mobile" />
    
    <parameter id="className"
    name="Activity Name"
    type="string"
    constraints="class|unique|nonempty"
    default="Name"
    help="The name of the functionality that requires ViveModel based acitity"/>
    
    <parameter
    id="activityLayoutName"
    name="Activity Layout Name"
    type="string"
    constraints="layout|unique|nonempty"
    suggest="${activityToLayout(className)}"
    default="activity_custom"
    help="The name of the layout to create for the fragment" />
    
    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />
</template>
